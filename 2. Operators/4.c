// 4. Increment & Decrement - 
// Create an integer variable with a value of 10.

// Perform and print the result after:

// Incrementing it by 1
// Incrementing it by 5
// Decrementing it by 2
// Decrementing it by 3

#include <stdio.h>

int main(){
    int num = 10;
    num += 1;
    num += 5;
    num -= 2;
    num -= 3;

    printf("%d", num);
    return 0;
}